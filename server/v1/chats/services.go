package v1Chats

//	It get the other user id value -> incase there is already a chat history.
//	Otherwise it returns the other userId
func UserTypingService(chatId string, userId string, ouserId string) {

}
